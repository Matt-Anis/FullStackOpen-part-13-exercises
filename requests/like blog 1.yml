info:
  name: like blog 1
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:3001/api/blogs/1/like
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
