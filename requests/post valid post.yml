info:
  name: post valid post
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:3001/api/blogs
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
