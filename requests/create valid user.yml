info:
  name: create valid user
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:3001/api/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
