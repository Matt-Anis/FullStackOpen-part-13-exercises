info:
  name: update reading list entry state
  type: http
  seq: 15

http:
  method: PUT
  url: http://localhost:3001/api/readingLists/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
