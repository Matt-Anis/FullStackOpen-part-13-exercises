info:
  name: "post bad blog "
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3001/api/blogs
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
