info:
  name: successfull login
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:3001/api/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
