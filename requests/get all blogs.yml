info:
  name: get all blogs
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3001/api/blogs
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
