info:
  name: "add a post to reading list of a user "
  type: http
  seq: 14

http:
  method: GET
  url: http://localhost:3001/api/readingLists
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
